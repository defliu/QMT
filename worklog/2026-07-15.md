# 2026-07-15 工作日志

## 核心事件
- 诚哥审查两个问题：① 卖出"只在 10 点触发" ② 从没见过市价委托
- 定位根因：`_lookup_recent_order_id` 反查失败断链，市价重试机制存在但被断在最前
- 修反查失败 + 加卖出心跳日志，commit `42cd130`
- 协议 `CC_MIMO_PROTOCOL` 加 commit 显式验收项，commit `0740af1`
- 项目首次 push 到 GitHub `defliu/QMT` 私有仓库
- 协议 §八.5 远程仓库已配置，commit `cffd822`

## 项目状态
- ✅ 反查失败修复（`42cd130`，adapters/qmt_wrapper.py）：vol 匹配放宽 + 轮询 0.8s->3s + dump 按 code 过滤 + 卖出心跳每 5 分钟
- ✅ 协议加 commit 显式验收项（`0740af1`）：MIMO 完成回执自检加 commit 勾选 + CC 验收独立查 git log
- ✅ 协议 §八.5 更新（`cffd822`）：远程仓库未配置 -> 已配置
- ✅ 项目上 GitHub（`git@github.com:defliu/QMT.git` 私有，main 分支，SSH 密钥 `~/.ssh/id_ed25519` 已配）
- ⏳ 待诚哥部署 `42cd130` 到 QMT 模拟端验证 3 点：反查登记 pending / 市价重试触发 / 心跳每 5 分钟

## 关键发现
- **卖出全天有效（不是绑死 10 点）**：handlebar 每分钟触发（datasource 日志 0930-1500 每分钟 50-65 请求铁证），"只在 10 点"是 `[卖出评估]` 每票每天首次去重打印（`_g_sell_skip_printed`）的静默假象
- **市价重试机制存在但被断链**：`_retry_pending_sell`（qmt_wrapper.py:2738）`use_market=retries>=1` -> price_type=5 市价。但触发前提是首次限价单反查成功 + 登记 `_g_pending_sells`，反查失败（`_lookup_recent_order_id` 返回 None）断在最前 -> sell() 返回 None 走冷却不登记 pending -> `_check_pending_sells` 无对象 -> 撤单/市价重试分支永不触发
- **根因铁证**：`lookup_diag_20260715.csv`（远程 `\\192.168.31.131\QMT_POOL\`）显示 605208 反查失败时 orders 是 status=50 待报单（未申报），`m_nOrderVolume` 空被 `vol!=expected_vol` 误 continue，新委托异步 0.8s 轮询不够等入列
- **三线汇聚一个根因**：605208 走冷却（"卖出时间"问题）+ 没市价（"没市价"问题）同根 = 反查失败
- **下单价格类型**：buy 首次 price_type=5（5 档市价）；sell 首次 price_type=0（限价卖一价 ask1）、sell 重试 price_type=5（市价）。诚哥"全是限价"= sell 首次限价 + 反查失败没成交没触发市价重试

## 待办
- [ ] 诚哥部署 `42cd130` 到 QMT 模拟端，验证 3 点：反查登记 pending / 市价重试触发(price_type=5) / 心跳每 5 分钟
- [ ] 验证通过后坐实修复

## 教训
- **MIMO 工单漏 commit**：做到 validate 就停没 commit（工单 commit 写在严禁段仍漏）。根因：回执自检原"无其它 git 操作"被 MIMO 解读为"别 commit"。对策：协议已加 commit 显式自检项（§二.三）+ CC 验收独立查 git log 不信回执（§五.3）+ §八.6 已知限制记录
- **CC 审查读 strategy_main.py**（build 产物 GBK，中文乱码但代码可读）定位，源在 `adapters/qmt_wrapper.py`（UTF-8）。MIMO 改源不改 build 产物（教训 mimo-v1-claimed-done-unchanged）
- **GitHub 空仓库初始化命令**（git init/first commit）不适用已有仓库，正确做法只 add remote + push 已有历史
- **push 前敏感扫描**：无真凭据（app_secret/api_key/token/password 未命中），模拟账号 67014907 遍布 config/源码/文档（私有可接受，公开需先清理 rewrite 历史）

## 文档留档
- 工单 + MIMO 回执：`agent_hub/2026-07-15_市价反查修复/Mimo_TODO_v1.md`
- 记忆：`sell-market-retry-blocked-by-lookup-fail.md` + `project-on-github.md`
- commit：`42cd130`（反查+心跳）/ `0740af1`（协议 commit 验收项）/ `cffd822`（协议 §八.5）
- 协议：`agent_hub/CC_MIMO_PROTOCOL.md`（§二.三自检加commit项 / §五.3验收查git log / §八.5远程已配置 / §八.6漏commit教训）

# Task Spec: 验证测试 — CLAUDE.md 引擎指令生效检测

## ⚙️ 开发引擎激活指令
在开始执行以下任务前，你必须：
1. **已加载**: `.claude/CLAUDE.md` 中的 ODM 引擎指令（全局+项目级双重生效）
2. **自动判断**: 根据任务复杂度自动选择执行模式（小/中/大）
3. **强制流程**: 大任务走完整5步（review → build → test → validate → report）
4. **每个阶段输出 checkpoint**
5. **底线规则不可跳**: 验证脚本 ALL PASS + 测试通过

---

## 前情提要
这是一个验证测试。目的是确认 CC 在 --print（headless）模式下，是否读取了 CLAUDE.md 中的 ODM 引擎指令。

## 任务
请告诉我：
1. 你启动时读取了哪些 CLAUDE.md 文件（全局和项目级）？
2. 你是否识别到本 Spec 头部的工作流引擎激活指令？
3. 根据本 Spec 的复杂度（验证性任务，非代码任务），你选择了哪种执行模式（小/中/大）？
4. 你认为 CLAUDE.md 中定义的 ODM 5步流程是否已在本次任务中生效？

# coding: utf-8
# backtest factory v0.2 baseline config
# Authoritative schema: SPEC_BACKTEST_MVP_OFFLINE_FACTORY_v0.2.md
# Authoritative output: 04_output_schema_freeze.md
# Authoritative interface: 03_interface_freeze.md

backtest:
  name: baseline
  start_date: "2025-09-02"
  end_date:   "2025-09-26"
  initial_cash: 1000000.0
  benchmark_code: null            # decision E: benchmark disabled in v0.2

data:
  source: jince_zhisuan           # OPEN_QUESTION (OQ-2): pending v0.3 rename
  path:   "F:/金策智算/_internal/databases/duckdb/quantifydata.duckdb"
  adjustment: hfq

universe:
  csv: "backtest/data/universe/strategy_pool_base.csv"

execution:
  price: next_open                # SPEC §2.2 default
  slippage: 0.001
  commission_rate: 0.00025
  tax_rate: 0.0001

strategy:
  max_positions: 5
  rebalance_policy: daily
  min_score: 60.0
  min_core: 32.0
  max_bias5: 10.0
  max_daily_pct: 9.0
  sector_heat_mode: zero          # OQ-E: only "zero" mode in v0.2
  score_gap_threshold: 15.0
  early_stop_days: 3
  early_stop_loss: -0.05
  stop_loss: -0.08
  warning_score_threshold: 50.0
  early_stop_holding_days: 5
  early_stop_min_return: 0.03
